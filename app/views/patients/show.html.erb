<h1><%= @patient.name %></h1>
<ul>
  <% @patient.appointments.each do |appt| %>
    
    <li><%= appt.display_date %> - <%= link_to appt.doctor.name, doctor_path(appt.doctor) %></li>
  <% end %>
</ul>