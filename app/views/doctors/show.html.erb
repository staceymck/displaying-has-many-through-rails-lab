<h1><%= @doctor.name %> - <%= @doctor.department %></h1>
<ul>
  <% @doctor.appointments.each do |appt| %>
    <li><%= link_to "#{appt.display_date} - #{appt.patient.name}", patient_path(appt.patient) %></li>
  <% end %>
</ul>